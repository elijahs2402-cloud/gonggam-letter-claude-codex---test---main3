# 7단계 제안서 — 색상 토큰 / 아이콘 체계 / 일러스트 분류 / 타이포 미적용

작성일 2026-08-31 · 브랜치 `design/system-integration` · **아직 아무것도 적용하지 않음**
모든 수치는 실측이며, 측정 방법을 각 항목에 적어 재현 가능하게 했다.

---

## 1. 색상 토큰 제안

### 실측

| 항목 | 수치 |
|---|---|
| 하드코딩 HEX | 87종 / 144회 |
| 하드코딩 rgba | **258종** / 582회 |
| rgba 기본색 | 49종 |
| 그중 기존 토큰에 대응되는 것 | **505/582회 = 87%** |

측정: `:root`를 제외한 `index.css` 본문에서 색 리터럴을 정규식으로 수집해
`:root`의 hex 토큰을 rgb로 변환한 대조표와 매칭.

### 진짜 문제는 "토큰이 없는 것"이 아니다

87%가 이미 토큰에 있는 색이다. 문제는 **같은 색의 알파 변형이 258종**이라는 것.

| 기본색 | 사용 | 알파 변형 |
|---|---|---|
| `--gold` (188,146,62) | 186회 | **56종** |
| `--deep-plum` | 52회 | 29종 |
| `--ink` | 91회 | 26종 |
| `--surface-card` | 53회 | 25종 |
| `--paper-light` | 50회 | 18종 |
| `--plum` | 23회 | 14종 |
| `--terracotta` | 17회 | 11종 |

금색의 알파 56종 중 일부:
```
.44  0.44  0.45  .45  .46  0.46  .48  0.48
```
**눈으로 구분 불가능한 값들이고, `.5`와 `0.5`처럼 표기까지 섞여 있다.**

### 제안: 알파 계단 6단 도입 + 기존 시맨틱 토큰 확장

이미 `--gold-rule-faint(.25) / -light(.32) / --frame-rule(.5) / -mid(.55) / -strong(.68)`이
있으나 **186회 중 대부분이 이를 쓰지 않고 직접 값을 적었다.** 새로 만들지 말고 이걸 살린다.

계단(실사용 분포에 맞춰):
```
05 / 12 / 25 / 40 / 55 / 70
```

| 색 | 제안 토큰 |
|---|---|
| 금색 | `--gold-a05 · a12 · a25 · a40 · a55 · a70` (기존 시맨틱 5종은 이 위에 별칭으로) |
| 자두 | `--plum-a08 · a16 · a25 · a40 · a55` |
| 잉크 | `--ink-a10 · a16 · a25 · a45 · a60` |
| 종이/카드 | `--paper-a70 · a85 · a95` (실사용이 .66~.98에 몰려 있음) |

**전환 규칙: 기존 값을 가장 가까운 계단으로 스냅.** 258종 → 약 20종.

### 토큰이 없는 색 (신설 검토)

| 값 | 사용 | 비고 |
|---|---|---|
| `rgb(255,255,255)` 순백 | 11회 | 종이 팔레트에 순백은 이질적. `--surface-card`로 흡수 검토 |
| `rgb(92,61,117)` | 6회 | `--deep-plum`보다 밝은 자두. `--plum`으로 흡수 가능한지 확인 필요 |
| `rgb(123,91,166)` | 5회 | 위와 같음 |
| `rgb(237,227,212)` | 5회 | `--paper-deep`(234,223,206)과 근접. 흡수 검토 |

### 위험

알파를 스냅하면 **테두리 진하기가 미세하게 바뀐다.** 특히 글자 뒤 배경으로 쓰인 경우
대비가 달라지므로, 적용 후 **28개 화면 대비 검사를 반드시 재실행**해야 한다(현재 위반 0건 유지 확인).

---

## 2. 아이콘 체계 제안

### 실측: 현재 5가지 방식이 섞여 있다

| 방식 | 사용 |
|---|---|
| CSS `mask` + `currentColor` | 3곳 (하단 내비) |
| CSS `url(/assets/...)` 배경 | 30곳 |
| CSS `content: "글리프"` | 14곳 |
| 인라인 SVG | 15곳 |
| `<img>` PNG | 38곳 |

글리프로 쓰인 문자: `←` `×` `›` `✉` `✻` `|` `“` `”` `—`

### 각 방식의 문제

- **텍스트 글리프** — 폰트에 따라 모양이 달라지고, 굵기 제어가 안 되며,
  크기가 타이포 척도를 오염시킨다. 실제로 `←` 24px·25px, `×` 25px, `›` 32px(굵기 300),
  `“` 52px이 **전부 척도 밖 값**으로 잡혔다.
- **`<img>` PNG / `url()` 배경** — 토큰 색을 따라갈 수 없다.
  hover·active·disabled마다 별도 파일이 필요해진다.
- **`mask` + `currentColor`** — 색이 토큰을 따라가고 상태가 자동으로 맞는다. 이미 내비에서 검증됨.

### 제안: 2가지로 수렴

| 용도 | 방식 | 이유 |
|---|---|---|
| **단색 UI 아이콘** (뒤로가기, 닫기, 화살표, 종, 봉투) | **CSS `mask` + `currentColor`** | 색·상태가 토큰을 자동으로 따름 |
| **다색 아이콘/작은 일러스트** | **인라인 SVG** | 색이 고정이어야 하는 것만 |

**텍스트 글리프를 아이콘으로 쓰는 것은 전면 폐기.**
단, `“ ” —`는 아이콘이 아니라 **조판 장식**이므로 그대로 둔다 (구분해야 함).

아이콘 크기 척도 신설:
```
--icon-sm: 16px   /* 행 안 보조 */
--icon-md: 20px   /* 기본 */
--icon-lg: 24px   /* 헤더 액션 */
```
터치 영역은 아이콘 크기와 무관하게 `--tap-min: 44px` 유지.

---

## 3. 일러스트 분류

### 실측

| 항목 | 수치 |
|---|---|
| `public/assets` 이미지 파일 | **413개** |
| 총 용량 | **616MB** |
| 코드에서 참조되는 것 | **42개** |
| 미참조 추정 | **약 371개** |

### 역할별 분류 (참조되는 42개)

| 분류 | 파일 | 성격 |
|---|---|---|
| **브랜드 히어로** | `intro-door-uploaded`(2.2M), `onboarding-new-hero`(1.5M), `onboarding-welcome-illustration`(516K) | 화면당 1개, 대형. 유지 |
| **상태 일러스트** | `listen-entry-empty-mailbox`(1.6M), `reply-review-open-letter-glasses`(1.1M), `read-letter-room-framed-two`(1004K), `reply-sent-paper-airplane`(668K), `reply-sent-lavender-envelope`(136K) | 빈 상태·대기·완료 |
| **상태 아이콘 세트** | `mailbox-status/*` 9개 | 편지함 상태 표시 |
| **UI 아이콘** | `home_icon`, `letter_icon`, `notebook_icon`, `notification-bell`, `decor.svg`, `account-provider-apple.svg` | → **2번 제안에 따라 mask 방식으로 전환 대상** |
| **감정 아이콘** | `emotion-*` 4개 | 감정 선택 |
| **텍스처** | `paper-pattern.jpg`(320K) | 배경 |
| **로그인 제공자** | `figma-login-apple/google`, `logo_toss` | 외부 브랜드. 임의 변경 금지 |
| **★ 시안 전용** | `direction-a-*`, `direction-b-*` 5개 = **약 5.8MB** | `/design-review`에서만 사용. **실서비스 아님** |

### 눈에 띄는 것

1. **`direction-*` 5.8MB가 프로덕션 번들에 포함된다.** 시안 비교용이라 실서비스 경로에서
   전혀 쓰이지 않는다. 빌드 제외 대상. (사용자 결정으로 보류 중인 항목과 연결됨)
2. **`mailbox-status/waiting.png`가 488K인데 형제 파일은 28~52K.** 같은 세트인데
   10배 차이 → 최적화 누락으로 보인다.
3. **미참조 371개 / 약 600MB.** 이전에 사용자 결정으로 정리 보류된 항목.

### 제안: 폴더를 역할로 나눈다

```
public/assets/
  brand/      히어로 (화면당 1개, 대형 허용)
  state/      빈 상태·대기·완료 일러스트
  icon/       단색 UI 아이콘 (SVG 우선)
  texture/    배경 텍스처
  provider/   외부 브랜드 (변경 금지)
  _review/    시안 전용 — 빌드 제외
```
파일 이동은 참조 경로를 함께 고쳐야 하므로 **한 번에, 검증과 묶어서** 해야 한다.

---

## 4. 타이포그래피 체계 미적용 페이지

### 측정 방법

28개 화면을 iframe으로 열고(렌더 완료 900ms 대기) 텍스트를 가진 모든 요소의
`fontSize / fontFamily / fontWeight` 계산값을 수집해 `:root` 토큰 집합과 대조.

### 결과: 28개 중 **21개 화면**에서 이탈, 고유 이탈 **30건**

### (a) `font-family: Pretendard` 하드코딩 — 15개 화면

`/mailbox` `/my-space` `/write-letter` `/read-letter/*` `/account-settings` `/service-guide`
`/onboarding` `/saved-excerpts` `/safety-management` `/mailbox-empty` `/login-information`
`/mailbox-my-replied-demo` `/account-restricted` `/data-and-privacy` `/terms-of-service`
`/anonymous-name-settings` `/account-withdrawal`

토큰은 `--font-sans: "Pretendard Variable", Pretendard, ...` 로 **Variable을 먼저** 요청한다.
하드코딩된 쪽은 정적 `Pretendard`를 먼저 요청한다.

**정직한 측정 결과: 이 기기에서는 두 폭이 모두 205.4px로 같다 — 눈에 보이는 차이가 없다.**
위험은 정적 Pretendard가 설치되지 않은 기기다. 그 경우 폴백이
`Noto Sans KR`(미로드) → `Malgun Gothic`(228.7px, **11% 넓음**)으로 떨어질 수 있다.
즉 **지금 깨져 보이지는 않지만 보장되지 않는다.**

→ 전부 `var(--font-sans)`로 치환. 위험 낮고 이득 명확.

### (b) 척도 밖 크기

| 크기 | 화면 | 판단 |
|---|---|---|
| **13px** | `/safety-management` `/report-letter-demo` `/data-and-privacy` `/account-withdrawal` `/notification-settings` | 본문 최소치 14px 미만. **14px로 올릴 것** |
| 20px | `/safety-management` "지금 도움이 필요하신가요?" | `--fs-section`(18) 또는 `--fs-state-title`(21)로 |
| 22px | `/login-information` | `--fs-state-title`(21)로 |
| 29px | `/intro` | `--fs-display`(30)로 |
| 24·25·32·52px | 글리프 `←` `×` `›` `“` | **2번 아이콘 제안에서 함께 해결** |

### (c) 굵기 300

`/account-settings`의 `›` 1건. 척도는 400/500/600/700. → 아이콘 전환으로 해소.

### (d) 죽은 토큰 `--font-brand`

`--font-brand: SUITE, ...`가 정의돼 있으나 **참조하는 규칙이 `:root` 하나뿐**
(= 실제 사용 0곳). SUITE 웹폰트가 로드된 것은 확인했다.

→ **둘 중 하나로 결정 필요**: 브랜드 폰트를 실제로 쓸 곳을 정하거나(예: 스플래시 로고),
쓰지 않을 거면 토큰과 `@font-face`를 함께 제거해 폰트 다운로드를 없앤다.
**이건 디자인 의도 결정이라 사용자 확인이 필요하다.**

---

## 적용 순서 제안

판단이 끝난 항목만 Sonnet 세션으로 넘길 수 있다.

| 순서 | 작업 | 위험 | 판단 필요? |
|---|---|---|---|
| 1 | (4-a) `Pretendard` → `var(--font-sans)` 15화면 | 낮음 | 아니오 — 바로 실행 가능 |
| 2 | (4-b) 13px → 14px, 20/22/29px 척도 스냅 | 낮음 | 아니오 |
| 3 | (1) 알파 계단 도입 + 258종 스냅 | **중간** | 계단 값 승인 필요 |
| 4 | (2) 아이콘 5방식 → 2방식 | **중간** | 어느 아이콘을 mask로 갈지 목록 승인 필요 |
| 5 | (3) 에셋 폴더 재편 | **높음** (경로 변경) | 승인 필요 |
| — | (4-d) `--font-brand` 존폐 | — | **사용자 결정** |

각 단계 후 필수 검증: 대비 28화면 0건 · 글자잘림/넘침/이탈 0건 · 타입에러 43개 유지.
