# 공감편지 디자인 작업 인계 문서

작성일: 2026-08-31 / 브랜치: `design/system-integration` / 마지막 커밋: `c12f612`

## 지금 상태 한 줄

6단계 디자인 시스템 정리를 **모두 마쳤고, 아무것도 커밋하지 않았다.**
다음 결정은 "커밋할지, 한다면 어떻게 쪼갤지"다.

## 커밋되지 않은 변경

| 파일 | 상태 |
|---|---|
| `src/index.css` | 대폭 수정 (약 +491 / -337). 이번 작업의 거의 전부 |
| `src/App.tsx` | 라우트 추가 (`/design-review/:dir/:screen`), `/onboarding` 교체 |
| `vite.config.ts` | `allowedHosts: ['.trycloudflare.com']` |
| `src/DesignReview.tsx`, `src/design-review.css` | 신규 (A/B/C 시안 비교 화면) |

추적되지 않은 PNG가 다수 있으나 **대부분 미참조 실험 이미지다. `git add -A` 금지.**
과거 두 번 이 실수로 수백 개 이미지를 커밋에 쓸어담았다. 반드시 경로를 하나씩 명시할 것.

## 완료한 6단계 (사용자 지시: "차례대로 수정해줘")

1. 같은 역할 제목 5종 → 통일
2. 세리프가 UI 영역 침범 → 해제 (본문/편지지에만 유지)
3. 12px 텍스트 과밀 해소
4. 타이포그래피 위계 확립 (**단, 날짜·상태·캡션은 12px 유지 — 사용자 명시 요구**)
5. 색상 토큰 정리
6. 카드·박스·버튼 체계 — 버튼 24종 → 11종

## 최종 검증 수치 (마지막 실행 기준)

- 대비 검사 28개 화면: **위반 0건**
- 가로 넘침 / 글자 잘림 / 화면 이탈 22개 화면: **각 0건**
- 터치 영역 34px 미만: **0건**
- 타입 에러: **43개** (전부 기존 것, 작업 전과 동일 — 늘지 않았는지가 판단 기준)

## 핵심 토큰 (src/index.css `:root`)

```
--btn-h: 56px          주요·보조 버튼 높이
--btn-h-text: 44px     텍스트 버튼 (터치 최소치)
--fs-button: 16px      버튼 글자
--fw-button: 600
--action-on-primary: #fffaf0
--disabled-text-on-fill: #605850   비활성 채움 위 글자 (대비 4.50:1)
--frame-rule: rgba(188,146,62,.5)  금색 테두리 1종
--paper: #f6f0e5  --surface-mat: #d8d0c3  --action-primary: #4e345e
```

버튼 통합 레이어는 **`src/index.css` 맨 끝**에 있다.
앞쪽에 같은 선택자가 2~9번 중복 정의돼 있어 캐스케이드로 이기게 한 것이므로,
버튼 스타일을 고칠 때는 앞쪽이 아니라 이 레이어를 수정할 것.

## 반드시 지킬 제약 (사용자가 명시)

- 처음부터 다시 디자인하지 말 것. 기능·화면·문구·상태·라우트를 임의 삭제 금지
- 금지: 글래스모피즘, 보라 그라데이션, 카드 둥근 모서리 전환, 이모지 아이콘,
  과한 그림자, 3D/광택 아이콘, 라일락 꽃 반복
- 기존 일러스트 재생성 금지. localStorage 구조를 디자인 목적으로 변경 금지
- 문구를 바꿔야 하면 원본과 제안을 나란히 놓고 근거를 먼저 제시
- **모서리는 0이 원칙. 예외는 필터 칩(999px)뿐**
- `--surface-mat: #d8d0c3`은 유지 (사용자 결정). 액자 매트라 의도적으로 어둡다

## 검증할 때 지킬 것 (실패에서 배운 것)

- **grep만 믿지 말 것.** 같은 선택자가 여러 번 정의돼 단일 수정이 무력화된다.
  브라우저 CSSOM에 "실제로 이기는 규칙"을 물어본 뒤 고칠 것
- **대비 측정은 렌더가 끝난 뒤에.** 대기 340ms로 쟀을 때 페이드인 중이던 요소가
  통째로 빠져 "위반 0건"이 나왔다. 실제로는 12건이었다. **900ms 이상 대기할 것**
- 대비 계산은 **알파 합성 필수**. 조상 배경을 단순 탐색하면 오탐이 난다
- 스윕 전 인증 확인. `gonggam_mock_auth_v1`이 없으면 대부분 경로가 `/login`으로 튕겨
  측정이 전부 무효가 된다. 스키마는 `src/mockAuth.ts`의 `MockUserAccount`

## 미해결 / 대기 중

- **커밋 여부 미결정** ← 다음 할 일
- A/B/C 시안 3종 (`/design-review/a|b|c/...`) — 사용자가 아직 선택 안 함
- 사용자 결정으로 보류(먼저 묻지 말 것): index.css 중복 선택자 정리,
  600MB 미참조 에셋 정리, `/prototype/` 빌드 제외, git 히스토리 재작성
- 드라이브 `gonggam-letter` 폴더 이미지 27개 중 13개 미확인

## 개발 서버

`.claude/launch.json`의 `gonggam-letter-app`, 포트 **8444**.
포트 8443은 형제 디렉터리(`gonggam-letter-claude-codex`)라 잘못된 앱이 뜬다.
