# 공감편지 작업 인계 — 2026-09-03

브랜치: `design/system-integration` / 마지막 커밋: `c12f612` / **커밋된 것 없음, 전부 작업 트리**

이 문서는 [`HANDOFF-design.md`](HANDOFF-design.md)(8월 31일, 디자인 시스템 6단계 정리)의 **후속**이다.
그 문서의 제약·토큰·금지사항은 여전히 유효하니 함께 읽을 것.

---

## 0. 다음 채팅이 가장 먼저 할 일

**답장 도착 상세의 '신고하기' 링크를 어떻게 고칠지 사용자 승인을 받는 것.**
아래 3장에 선택지 두 개가 정리돼 있다. 이것 말고는 대기 중인 작업이 없다.

---

## 1. 이 세션에서 한 일 (검증 완료)

### 편지지 안 '신고하기' 색상 통일

두 화면(`/read-letter/:id`, `/mailbox/my/:id` 답장 도착)의 신고하기 링크가
행동색인 보라(`--deep-plum #4E345E`)라서 차분한 편지지 위에서 가장 눈에 띄는 요소였다.
사용자 지시로 같은 화면의 "짧은 한마디도 누군가에게 힘이 될 수 있어요." 문구 색으로 맞췄다.

적용 위치: `src/index.css` 맨 끝 (9955줄~)

```css
.active-reading-report,
.my-letter-reply-report .flow-text-button,
.my-letter-reply-paper .my-letter-reply-report .flow-text-button {
  color: rgb(99, 85, 73);
  font-size: 14px;
  text-decoration-line: underline;
}
```

**브라우저 실측 결과 (두 화면 일치 확인):**

| 화면 | 색 | 크기 | 밑줄 |
|---|---|---|---|
| `/read-letter/sample-waiting-letter-one` | `rgb(99, 85, 73)` | 14px | underline |
| `/mailbox/my/demo-arrived-read` | `rgb(99, 85, 73)` | 14px | underline |

세 번째 선택자가 필요했던 이유: `index.css:7247`의
`.my-letter-reply-paper .my-letter-reply-report .flow-text-button`(무게 0,3,0)이
`--faint-ink-on-mat`(`rgb(101,86,69)`)을 걸고 있어 0,2,0 규칙을 이겼다.

### 더 연하게 하자는 제안은 반려됨 (근거)

사용자가 "더 연하게 할까?"라 물어 대비값을 근거로 만류했고, 현재 값 유지로 정리됐다.

| 색 | 편지지 배경 대비 | 판정 |
|---|---|---|
| `rgb(99,85,73)` (현재) | 약 6.3 : 1 | 적정 |
| `--muted-ink #766C61` | 약 4.6 : 1 | AA 통과선 바로 위 (낮출 경우 여기까지) |
| `--faint-ink #9A9083` | 약 2.9 : 1 | **AA 미달 — 쓰지 말 것** |

신고는 급할 때 눈으로 찾아야 하는 기능이라 더 낮추면 안 된다.

---

## 2. 이 세션에서 겪은 함정 (반복될 수 있음)

**CSS 주석 `*/` 중복으로 파싱이 깨졌다.**
기존 주석 블록에 설명을 덧붙이면서 `*/`를 지우지 않아, 그 아래 규칙 전체가 무시됐다.
증상은 "규칙을 넣었는데 값이 안 바뀜"이라 특이도 문제와 구별이 안 된다.

**편집 후 반드시 확인할 것:**

```bash
python -c "
import re
s=open('src/index.css',encoding='utf-8').read()
body=re.sub(r'/\*.*?\*/','',s,flags=re.S)
print('여는{',body.count('{'),'닫는}',body.count('}'))
"
```

현재 정상값은 **2295 / 2295**.
(`/*` 개수와 `*/` 개수는 1 차이가 나는 게 정상이다 — CSS 값 안에 `*/` 문자열이 있어서 그렇다. 이건 오류가 아니다.)

**받은 답장 편지지가 안 보인다고 착각했다.**
스크린샷에 안 나와서 렌더 실패로 오해했으나, 실제로는 스크롤 아래에 있었다
(스크롤 높이 1327 / 보이는 높이 750, 편지지 top 770).
스크린샷은 뷰포트만 찍는다 — 없다고 단정하기 전에 `scrollTop = scrollHeight` 후 재확인할 것.

---

## 3. 미결 결정 — 신고하기 링크 (여기서 멈춤)

### 문제

| 화면 | 현재 링크 | 도착 화면 |
|---|---|---|
| 편지 읽기 (`/read-letter/:id`) | `/report-letter/:id` | `LetterReportFigmaScreen` — **최종 디자인** |
| 답장 도착 상세 (`/mailbox/my/:id`) | `/report-reply/:id` | `ReplyReportScreen` — 옛 미디자인 화면 |

사용자 지시: **"신고하기는 `report-letter/sample-waiting-letter-one` 이 페이지가 최종이야"**

### 링크만 바꾸면 안 되는 이유

`LetterReportFigmaScreen`(`src/ReportScreens.tsx`)은 **편지**를 신고하도록 짜여 있다:

```
targetType: "letter",  targetId: letter.id      → 내가 쓴 편지를 신고
blockedUserId: letter.senderId                   → senderId 는 '나' → 나를 차단
hideContent(userId, "letter", letter.id)         → 내가 보낸 편지를 숨김
```

답장 도착 화면에서 `letter.senderId`는 **사용자 본인**이다. 신고 대상은 상대가 쓴 답장인데
위 코드는 내 편지를 신고하고 나를 차단한다. `existing` 가드에도 걸려
"이미 신고한 편지예요"가 뜰 수 있다.

### 선택지

**가 (권장) — 화면만 최종 디자인으로, 대상은 답장 유지**
`ReplyReportScreen`(`src/SafetyActionScreens.tsx:13`)의 렌더 부분을
`LetterReportFigmaScreen`과 같은 마크업(`figma-report-screen`, `figma-report-detail`,
`figma-report-setting`, `flow-fixed-action--split figma-report-action`)으로 교체.
신고 대상은 `reply.id`, 차단 대상은 `reply.writerId` 그대로.
→ 화면은 최종본과 동일, 동작도 올바름. URL만 `/report-reply/:id`로 남음.

**나 — 링크만 교체**
`src/LetterFlowScreens.tsx:564`의 `` `/report-reply/${...}` `` → `` `/report-letter/${...}` ``.
1줄이지만 위 3가지 오작동이 그대로 남는다.

참고: `ReplyReportScreen`에만 있는 신고 사유 `irrelevant_or_insincere`
("편지와 무관하거나 성의 없는 답장")는 답장 전용이다. 가 안에서는 유지할 것.

---

## 4. 남은 과제 (사용자 답변 대기 / 미착수)

| 항목 | 상태 |
|---|---|
| 미리보기 상단 일러스트가 편지 쓰기보다 51px 아래, 비율도 다름(1.39 vs 1.50이 125×98 박스를 채움) | 선택지 제시했으나 사용자 미응답 |
| `getMailboxPreviewRecords`(`src/MailboxScreen.tsx:79`) 정의만 남고 미사용 | 삭제 제안했으나 미응답 |
| 편지함 표본 데이터가 **localStorage 에만** 있음 (`demo-arrived-read` 등은 소스에 없음) | 코드로 옮기자고 제안, 미응답 |
| 답장 도착 상세의 **차단·숨김 상태**는 일부러 옛 레이아웃으로 보냄 | 한 번도 렌더 확인 안 함 |
| `/reply-arrived/:id` 라우트 유지 (`src/App.tsx:503`) | 사용자 계획: 나중에 홈 알림에서 사용 |
| iOS 키보드 커서 가림 — `scroll-padding-bottom` (1단계) 적용 | **실제 아이폰 미검증**. 2단계(JS 캐럿 스크롤) 대기 |
| 하단 바 높이 미통일: `reading-fixed-action` 73px, `reply-preview-fixed-actions` 71px, `home-scene-actions` 103px | 표준은 80px |
| `app-bottom-navigation`, `auth-actions` 는 아직 95% 반투명 | 다른 바는 불투명 처리됨 |
| `index.html:6` 뷰포트에 `viewport-fit=cover` 없음 | `env(safe-area-inset-*)` 56곳이 0을 반환 중 |
| 프로토타입 화면 `/prototype/anon-name-mockup-2`, `-3` 은 아직 12자 | 본 화면은 10자로 변경됨 |

---

## 5. 작업 환경 · 규칙

- 개발 서버: **포트 6334**. 브라우저 도구로 `preview_start` 또는 `navigate` 사용. Bash 로 서버 띄우지 말 것
- 라우팅: `src/App.tsx`가 `location.pathname`을 읽는 단일 파일 라우터.
  이동은 SPA pushState 가 아니라 **전체 페이지 로드**(`window.location.href`)
- **SPA 라 모든 경로가 HTTP 200 을 준다.** `fetch` 로 검증하면 거짓 성공이 난다.
  반드시 렌더시키고 "편지를 찾을 수 없어요"가 뜨는지 눈으로 확인할 것
- `src/index.css`는 9975줄이고 같은 선택자가 여러 겹으로 중복 정의돼 있다.
  **grep 만으로 어떤 규칙이 이기는지 알 수 없다.** CSSOM 에 직접 물어볼 것:
  ```js
  getComputedStyle(el).color   // 실제 적용값
  ```
  이번 세션에서만 특이도 함정에 5번 걸렸다
- 새 규칙은 **파일 맨 끝에 블록으로 추가**하는 것이 이 저장소의 관행이다
- 측정 전에 `*{transition:none!important}` 를 주입할 것 (스크린샷이 페이드 중간에 찍힌다)
- 모의 로그인 복구 조건: `state: "logged_in"` **그리고** `account.onboardingCompleted` 둘 다 필요
  (`src/mockAuth.ts:242`). localStorage 는 origin 단위라 포트가 바뀌면 초기화된다

### 커밋 시 주의 (이전 문서에서 이어짐)

작업 트리에 **92개 변경**이 있고 그중 상당수가 미참조 실험 PNG 다.
**`git add -A` 금지.** 과거 두 번 이 실수로 수백 개 이미지를 커밋에 쓸어담았다.
경로를 하나씩 명시할 것.

이 세션이 만진 소스 파일은 `src/index.css` 하나다.

---

## 6. 확실하지 않은 것

- 대비값 6.3:1 / 4.6:1 / 2.9:1 은 계산값이며 별도 검사 도구로 재확인하지 않았다
- iOS `scroll-padding-bottom` 수정은 실제 아이폰에서 검증되지 않았다
- 답장 도착 상세의 차단·숨김 분기는 렌더 확인을 하지 않았다
- 선택지 '가'의 작업량은 렌더 부분 교체 기준 추정이며, `figma-report-*` 스타일이
  `report-screen` 계열 클래스에 의존하는지는 확인하지 않았다
