# 공감편지 — 프로젝트 규칙

개발팀 규격 문서(저장소 밖): `C:/Users/elija/Documents/네이트온 받은 파일/PROJECT_SPEC.md`

## 디자인 동결 규칙 (PROJECT_SPEC.md 보다 우선)
이 작업은 새로 퍼블리싱하는 것이 아니라, 완성된 화면을 개발팀 규격에 맞게 리팩터링하는 것이다.
1. 현재 앱이 그리는 화면이 곧 디자인 시안이다. 모든 변경은 변경 전과 같은 화면을 내야 한다.
2. PROJECT_SPEC.md 중 다음은 적용하지 않는다:
   §2 디자인 분석·퍼블리싱 / §8 예시 토큰 값 / §31 작업 순서 1~7단계(화면·컴포넌트 재정의와 재구현)
3. 기존 디자인 토큰(--paper, --deep-plum 등)의 이름과 값을 바꾸지 않는다.
4. CSS를 옮길 때 선택자·선언 순서·우선순위(!important 포함)를 그대로 유지한다.
   비슷한 값끼리 합치거나 "정리"하지 않는다.
5. Tailwind를 제거할 때는 Preflight와 같은 초기화 규칙을 globals.css에 먼저 옮겨 둔다.
6. 한 번에 한 화면씩 옮기고, 전후 스크린샷(375×667, 390×844, 430×932)이 같을 때만 다음으로 넘어간다.
7. 차이가 생기면 멈추고 보고한다. 임의로 디자인을 고쳐 맞추지 않는다.
